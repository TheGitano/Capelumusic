[phases.setup]
nixPkgs = ["python310", "ffmpeg"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python bot_musical.py"
```

### 4️⃣ .gitignore (ARCHIVO NUEVO - MUY IMPORTANTE)
Crea un archivo `.gitignore` y pega esto:
```
downloads/
*.mp3
*.mp4
venv/
__pycache__/
*.pyc
.env
